name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  # required
  - pandas
  - scipy
  - xarray
  - pyproj>=2
  - netcdf4
  - rasterio
  # testing
  - pytest
  - pytest-cov
  - pip:
    - codecov
