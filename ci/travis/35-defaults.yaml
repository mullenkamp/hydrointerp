name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.5
  # required
  - pandas
  - scipy
  - xarray
  - pyproj>=2.1
  - netcdf4
  # testing
  - pytest
  - pytest-cov
  - pip:
    - codecov
